# Health check endpoints - restricted to localhost only
# These endpoints are used by Kubernetes liveness/readiness probes and Docker healthchecks
# They should not be accessible from the internet

Require local

# Enable URL rewriting for clean paths
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
