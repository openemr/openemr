services:
  selenium:
    image: selenium/standalone-chromium:151.0.7922.108@sha256:1d3d834a2ce93f26cc0d0ae3c61abd189755b32649f5c356c6c5cf9502aa397e
    # Services with profiles don't start by default.
    profiles:
    - selenium
    - video-recording
    ports:
    - 4444:4444  # Selenium WebDriver interface
    environment:
      SE_SCREEN_WIDTH: 1920
      SE_SCREEN_HEIGHT: 1080
      SE_SCREEN_DEPTH: 24
      SE_START_XVFB: true
    volumes:
    - /dev/shm:/dev/shm
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:4444/wd/hub/status
      start_period: 30s
      interval: 30s
      timeout: 5s
      retries: 3
  video:
    image: selenium/video:ffmpeg-7.1-20250808@sha256:c418f6e20e3496c393ad501a0f599813e08d50dc8b88ffb00fb6727bc7bd8aa3
    # Services with profiles don't start by default.
    profiles:
    - video-recording
    depends_on:
    - selenium
    environment:
      DISPLAY_CONTAINER_NAME: selenium
  openemr:
    environment:
      SELENIUM_USE_GRID: "true"
      SELENIUM_HOST: selenium
      SELENIUM_FORCE_HEADLESS: "false"
