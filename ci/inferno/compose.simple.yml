services:
  inferno:
    build:
      context: onc-certification-g10-test-kit
    volumes:
      - ./onc-certification-g10-test-kit/data:/opt/inferno/data
    environment:
      INFERNO_DISABLE_TLS_TEST: 'true'
      FHIR_RESOURCE_VALIDATOR_URL: http://localhost:3500
    network_mode: host

  hl7_validator_service:
    image: infernocommunity/inferno-resource-validator:1.0.73
    network_mode: host
    command:
      - java
      - -server
      - -XX:+UnlockExperimentalVMOptions
      - -XX:InitialRAMPercentage=79
      - -XX:MinRAMPercentage=79
      - -XX:MaxRAMPercentage=79
      - -XX:+UseG1GC
      - -XX:MaxGCPauseMillis=100
      - -XX:+UseStringDeduplication
      - -XX:+CrashOnOutOfMemoryError
      - -jar
      - validator-wrapper.jar
      - -startServer
      - -tx
      - n/a
    environment:
      SESSION_CACHE_DURATION: -1
    volumes:
      - ./onc-certification-g10-test-kit/lib/onc_certification_g10_test_kit/igs:/home/igs
