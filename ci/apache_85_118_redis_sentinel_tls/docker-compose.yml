##
# Redis Sentinel with TLS (server certificate only).
#
# Extends the shared redis-sentinel template with TLS on all Redis and Sentinel
# ports.  Clients verify the server certificate but are NOT required to present
# a client certificate (tls-auth-clients no).
#
# A one-shot init container generates a self-signed CA + server cert (with SANs
# covering every container hostname) into a shared Docker volume that all
# services mount read-only.

x-includes:
  node-version: "24"
  redis-sentinel-template: "compose-shared-redis-sentinel/docker-compose.yml"
  selenium-template: "compose-shared-selenium/docker-compose.yml"
  webserver-template: "compose-shared-apache.yml"
  database-template: "compose-shared-mariadb.yml"
  mailpit-template: "compose-shared-mailpit/compose.yml"

services:
  # ---------- cert generation ----------
  redis-tls-init:
    image: alpine:3.23
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -ex

        # Idempotent: skip if certs already exist.  docker compose up may
        # restart this container on subsequent calls; regenerating would
        # invalidate certs the running Redis/Sentinel containers already loaded.
        if [ -f /certs/ca.crt ]; then
            echo "Certs already present — skipping generation."
            exit 0
        fi

        apk add --no-cache openssl
        cd /certs

        # --- CA ---
        openssl genrsa -out ca.key 2048
        openssl req -x509 -new -key ca.key -days 1 -out ca.crt -subj "/CN=test-ca"

        # --- Server cert (covers every Redis/Sentinel hostname) ---
        echo "subjectAltName=DNS:redis-master,DNS:redis-replica-1,DNS:redis-replica-2,DNS:sentinel-1,DNS:sentinel-2,DNS:localhost" > /tmp/san.ext
        openssl genrsa -out server.key 2048
        openssl req -new -key server.key -subj "/CN=redis" \
          | openssl x509 -req -CA ca.crt -CAkey ca.key -CAcreateserial \
              -extfile /tmp/san.ext -days 1 -out server.crt

        # --- Client cert (for mTLS variant; harmless to generate always) ---
        openssl genrsa -out client.key 2048
        openssl req -new -key client.key -subj "/CN=openemr-client" \
          | openssl x509 -req -CA ca.crt -CAkey ca.key -CAcreateserial \
              -days 1 -out client.crt

        # --- Symlinks matching SentinelUtil's expected filenames ---
        ln -sf ca.crt redis-sentinel-ca
        ln -sf client.crt redis-sentinel-cert
        ln -sf client.key redis-sentinel-key
        ln -sf ca.crt redis-master-ca
        ln -sf client.crt redis-master-cert
        ln -sf client.key redis-master-key

        chmod 644 *.crt *.key ca.key
        ls -la /certs/
    volumes:
      - redis-certs:/certs

  # ---------- Redis overrides: enable TLS, disable plain TCP ----------
  redis-master:
    command:
      - redis-server
      - --bind
      - "0.0.0.0"
      - --protected-mode
      - "no"
      - --appendonly
      - "yes"
      - --requirepass
      - strongpassword
      - --masterauth
      - strongpassword
      - --tls-port
      - "6379"
      - --port
      - "0"
      - --tls-cert-file
      - /certs/server.crt
      - --tls-key-file
      - /certs/server.key
      - --tls-ca-cert-file
      - /certs/ca.crt
      - --tls-auth-clients
      - "no"
      - --tls-replication
      - "yes"
      - --replica-announce-ip
      - redis-master
    volumes:
      - redis-certs:/certs:ro
    depends_on:
      redis-tls-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "redis-cli", "--tls", "--cacert", "/certs/ca.crt",
             "-a", "strongpassword", "ping"]

  redis-replica-1:
    command:
      - redis-server
      - --bind
      - "0.0.0.0"
      - --protected-mode
      - "no"
      - --appendonly
      - "yes"
      - --requirepass
      - strongpassword
      - --masterauth
      - strongpassword
      - --replicaof
      - redis-master
      - "6379"
      - --tls-port
      - "6379"
      - --port
      - "0"
      - --tls-cert-file
      - /certs/server.crt
      - --tls-key-file
      - /certs/server.key
      - --tls-ca-cert-file
      - /certs/ca.crt
      - --tls-auth-clients
      - "no"
      - --tls-replication
      - "yes"
      - --replica-announce-ip
      - redis-replica-1
    volumes:
      - redis-certs:/certs:ro
    depends_on:
      redis-tls-init:
        condition: service_completed_successfully

  redis-replica-2:
    command:
      - redis-server
      - --bind
      - "0.0.0.0"
      - --protected-mode
      - "no"
      - --appendonly
      - "yes"
      - --requirepass
      - strongpassword
      - --masterauth
      - strongpassword
      - --replicaof
      - redis-master
      - "6379"
      - --tls-port
      - "6379"
      - --port
      - "0"
      - --tls-cert-file
      - /certs/server.crt
      - --tls-key-file
      - /certs/server.key
      - --tls-ca-cert-file
      - /certs/ca.crt
      - --tls-auth-clients
      - "no"
      - --tls-replication
      - "yes"
      - --replica-announce-ip
      - redis-replica-2
    volumes:
      - redis-certs:/certs:ro
    depends_on:
      redis-tls-init:
        condition: service_completed_successfully

  # ---------- Sentinel overrides ----------
  sentinel-1:
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        printf '%s\n' \
          'port 0' \
          'tls-port 26379' \
          'tls-cert-file /certs/server.crt' \
          'tls-key-file /certs/server.key' \
          'tls-ca-cert-file /certs/ca.crt' \
          'tls-auth-clients no' \
          'tls-replication yes' \
          'daemonize no' \
          'sentinel resolve-hostnames yes' \
          'sentinel announce-hostnames yes' \
          'sentinel monitor redis-master redis-master 6379 2' \
          'sentinel auth-pass redis-master strongpassword' \
          'sentinel down-after-milliseconds redis-master 5000' \
          'sentinel failover-timeout redis-master 60000' \
          'sentinel parallel-syncs redis-master 1' \
          > /tmp/sentinel.conf && redis-sentinel /tmp/sentinel.conf
    volumes:
      - redis-certs:/certs:ro
    depends_on:
      redis-tls-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "redis-cli", "--tls", "--cacert", "/certs/ca.crt",
             "-p", "26379", "SENTINEL", "masters"]

  sentinel-2:
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        printf '%s\n' \
          'port 0' \
          'tls-port 26379' \
          'tls-cert-file /certs/server.crt' \
          'tls-key-file /certs/server.key' \
          'tls-ca-cert-file /certs/ca.crt' \
          'tls-auth-clients no' \
          'tls-replication yes' \
          'daemonize no' \
          'sentinel resolve-hostnames yes' \
          'sentinel announce-hostnames yes' \
          'sentinel monitor redis-master redis-master 6379 2' \
          'sentinel auth-pass redis-master strongpassword' \
          'sentinel down-after-milliseconds redis-master 5000' \
          'sentinel failover-timeout redis-master 60000' \
          'sentinel parallel-syncs redis-master 1' \
          > /tmp/sentinel.conf && redis-sentinel /tmp/sentinel.conf
    volumes:
      - redis-certs:/certs:ro
    depends_on:
      redis-tls-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "redis-cli", "--tls", "--cacert", "/certs/ca.crt",
             "-p", "26379", "SENTINEL", "masters"]

  # ---------- OpenEMR: add TLS env vars + cert volume ----------
  openemr:
    image: openemr/openemr:flex-3.23-php-8.5@sha256:e4562b0c7d3f222ec8f72122ce00d10ffa93f559c38c00ab12c1355394c35d1c
    environment:
      REDIS_TLS: "yes"
      REDIS_TLS_CERT_KEY_PATH: /redis-certs
    volumes:
      - redis-certs:/redis-certs:ro
    depends_on:
      redis-tls-init:
        condition: service_completed_successfully

  mysql:
    image: mariadb:11.8.6@sha256:78a5047d3ba33975f183f183c2464cc7f1eab13ec8667e57cc9a5821d6da7577

volumes:
  redis-certs:
